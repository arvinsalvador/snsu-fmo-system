<x-app-layout>
    <x-slot name="header"><div class="flex w-full items-center justify-between gap-4"><div><p class="text-sm font-medium text-emerald-700">Administration / Staff</p><h1 class="text-2xl font-semibold text-gray-900">{{ $profile->user?->name }}</h1></div><div class="flex gap-2"><a href="{{ route('admin.staff.skills.edit', $profile) }}" class="rounded-md border border-gray-300 px-4 py-2 text-sm font-semibold text-gray-700">Manage skills</a><a href="{{ route('admin.staff.edit', $profile) }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Edit profile</a></div></div></x-slot>
    <div class="mx-auto max-w-4xl space-y-5">@if(session('success'))<div class="rounded-md border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif @include('admin.partials.tabs')
        <div class="rounded-md border border-gray-200 bg-white"><dl class="grid sm:grid-cols-2">@foreach(['Employee code'=>$profile->employee_code,'Email'=>$profile->user?->email,'Position'=>$profile->position,'Designation'=>$profile->designation,'Employment status'=>Str::headline($profile->employment_status),'Availability'=>Str::headline($profile->availability_status),'Skills'=>$profile->skills->pluck('name')->implode(', '),'Remarks'=>$profile->remarks] as $label=>$value)<div class="border-b border-gray-100 px-5 py-4"><dt class="text-xs font-semibold uppercase text-gray-500">{{ $label }}</dt><dd class="mt-1 text-sm font-medium text-gray-900">{{ $value ?: '—' }}</dd></div>@endforeach</dl><div class="px-5 py-4"><a href="{{ route('admin.staff.index') }}" class="text-sm font-semibold text-gray-600">Back to staff profiles</a></div></div>
    </div>
</x-app-layout>
