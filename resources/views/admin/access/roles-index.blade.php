<x-app-layout>
    <x-slot name="header"><div class="flex w-full items-center justify-between gap-4"><div><p class="text-sm font-medium text-emerald-700">Administration / Access</p><h1 class="text-2xl font-semibold text-gray-900">Roles</h1></div><a href="{{ route('admin.roles.create') }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Add role</a></div></x-slot>
    <div class="space-y-5">@if(session('success'))<div class="rounded-md border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif @include('admin.partials.tabs')
        <form method="GET" class="grid gap-3 border-b border-gray-200 pb-5 sm:grid-cols-[1fr_11rem_9rem_auto]"><input name="search" value="{{ $filters['search'] ?? '' }}" placeholder="Search roles" aria-label="Search roles" class="rounded-md border-gray-300 text-sm"><select name="sort" aria-label="Sort roles" class="rounded-md border-gray-300 text-sm"><option value="name">Name</option><option value="created_at" @selected(($filters['sort'] ?? '')==='created_at')>Created date</option></select><select name="per_page" aria-label="Rows per page" class="rounded-md border-gray-300 text-sm">@foreach([10,15,25,50] as $number)<option value="{{ $number }}" @selected((int)($filters['per_page'] ?? 15)===$number)>{{ $number }} rows</option>@endforeach</select><button class="rounded-md bg-gray-900 px-4 py-2 text-sm font-semibold text-white">Apply</button></form>
        <div class="flex justify-between"><p class="text-sm text-gray-500">{{ $roles->total() }} roles</p><a href="{{ route('admin.roles.export', request()->query()) }}" class="text-sm font-semibold text-gray-700">Export CSV</a></div>
        <div class="overflow-hidden rounded-md border border-gray-200 bg-white"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left">Role</th><th class="px-4 py-3 text-left">Users</th><th class="px-4 py-3 text-left">Permissions</th><th class="px-4 py-3 text-right">Actions</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($roles as $role)<tr><td class="px-4 py-3 font-semibold">{{ $role->name }}</td><td class="px-4 py-3">{{ $role->users_count }}</td><td class="px-4 py-3 text-gray-600">{{ $role->permissions->count() }}</td><td class="px-4 py-3 text-right"><a href="{{ route('admin.roles.show', $role) }}" class="font-medium text-gray-600">View</a>@if($role->name !== 'Super Admin')<a href="{{ route('admin.roles.edit', $role) }}" class="ml-3 font-medium text-emerald-700">Edit</a>@endif</td></tr>@empty<tr><td colspan="4" class="px-4 py-12 text-center text-gray-500">No roles match the current search.</td></tr>@endforelse</tbody></table></div>{{ $roles->links() }}
    </div>
</x-app-layout>
