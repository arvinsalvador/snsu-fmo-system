<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-medium text-emerald-700">Administration / Access / Permissions</p><h1 class="text-2xl font-semibold text-gray-900">{{ Str::headline($permission->name) }}</h1></div></x-slot>
    <div class="mx-auto max-w-3xl space-y-5">@include('admin.partials.tabs')<div class="rounded-md border border-gray-200 bg-white"><dl class="grid sm:grid-cols-2"><div class="border-b border-gray-100 px-5 py-4"><dt class="text-xs font-semibold uppercase text-gray-500">Permission key</dt><dd class="mt-1"><code class="text-sm">{{ $permission->name }}</code></dd></div><div class="border-b border-gray-100 px-5 py-4"><dt class="text-xs font-semibold uppercase text-gray-500">Assigned roles</dt><dd class="mt-1 text-sm font-medium">{{ $permission->roles_count }}</dd></div></dl><div class="px-5 py-4"><h2 class="text-sm font-semibold">Roles using this permission</h2><div class="mt-3 flex flex-wrap gap-2">@forelse($permission->roles->sortBy('name') as $role)<a href="{{ route('admin.roles.show', $role) }}" class="rounded-full bg-gray-100 px-3 py-1 text-sm text-gray-700">{{ $role->name }}</a>@empty<p class="text-sm text-gray-500">No roles currently use this permission.</p>@endforelse</div></div><div class="border-t border-gray-100 px-5 py-4"><a href="{{ route('admin.permissions.index') }}" class="text-sm font-semibold text-gray-600">Back to permissions</a></div></div></div>
</x-app-layout>
