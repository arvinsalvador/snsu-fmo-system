<x-app-layout>
    <x-slot name="header"><div class="flex w-full items-center justify-between gap-4"><div><p class="text-sm font-medium text-emerald-700">Administration / Access / Roles</p><h1 class="text-2xl font-semibold text-gray-900">{{ $role->name }}</h1></div>@if($role->name !== 'Super Admin')<a href="{{ route('admin.roles.edit', $role) }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Edit role</a>@endif</div></x-slot>
    <div class="mx-auto max-w-4xl space-y-5">@if(session('success'))<div class="rounded-md border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif @include('admin.partials.tabs')
        <div class="rounded-md border border-gray-200 bg-white"><div class="grid border-b border-gray-100 sm:grid-cols-2"><div class="px-5 py-4"><p class="text-xs font-semibold uppercase text-gray-500">Assigned users</p><p class="mt-1 text-xl font-semibold">{{ $role->users_count }}</p></div><div class="px-5 py-4"><p class="text-xs font-semibold uppercase text-gray-500">Permissions</p><p class="mt-1 text-xl font-semibold">{{ $role->permissions->count() }}</p></div></div><div class="px-5 py-4"><h2 class="text-sm font-semibold">Granted permissions</h2><div class="mt-3 flex flex-wrap gap-2">@forelse($role->permissions->sortBy('name') as $permission)<span class="rounded-full bg-gray-100 px-3 py-1 text-sm text-gray-700">{{ Str::headline($permission->name) }}</span>@empty<p class="text-sm text-gray-500">This role has no permissions.</p>@endforelse</div></div><div class="border-t border-gray-100 px-5 py-4"><a href="{{ route('admin.roles.index') }}" class="text-sm font-semibold text-gray-600">Back to roles</a></div></div>
    </div>
</x-app-layout>
