<x-app-layout><x-slot name="header"><h1 class="text-2xl font-semibold">{{ $target->definition->name }}</h1></x-slot><div class="rounded-lg border bg-white p-6"><p>Target: <strong>{{ $target->target_value }}</strong> · Owner: {{ $target->owner?->name??'Unassigned' }}</p>@can('evaluate',$target)<form method="POST" action="{{ route('admin.kpi-targets.evaluate',$target) }}" class="mt-4">@csrf<button class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Evaluate now</button></form>@endcan</div><section class="mt-6 rounded-lg border bg-white p-6"><h2 class="font-semibold">Evaluation history</h2><table class="mt-4 min-w-full text-sm"><tr><th>Date</th><th>Actual</th><th>Score</th><th>Status</th><th>Trend</th></tr>@foreach($target->evaluations as $e)<tr><td>{{ $e->evaluation_date->toDateString() }}</td><td>{{ $e->actual_value }}</td><td>{{ $e->score }}</td><td>{{ $e->status }}</td><td>{{ $e->trend_direction }}</td></tr>@endforeach</table></section></x-app-layout>
