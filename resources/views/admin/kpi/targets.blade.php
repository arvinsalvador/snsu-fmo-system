<x-app-layout><x-slot name="header"><div class="flex justify-between"><h1 class="text-2xl font-semibold">KPI Targets</h1>@can('create',App\Models\KpiTarget::class)<a href="{{ route('admin.kpi-targets.create') }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">New target</a>@endcan</div></x-slot><div class="grid gap-4">@foreach($targets as $t)<a href="{{ route('admin.kpi-targets.show',$t) }}" class="rounded-lg border bg-white p-5"><div class="flex justify-between"><strong>{{ $t->definition->name }}</strong><span>{{ str($t->current_evaluation_status??'no_data')->replace('_',' ')->title() }}</span></div><p class="mt-2 text-sm text-gray-500">{{ $t->period_start->toDateString() }} to {{ $t->period_end->toDateString() }} · Owner: {{ $t->owner?->name??'Unassigned' }}</p></a>@endforeach</div><div class="mt-4">{{ $targets->links() }}</div></x-app-layout>
