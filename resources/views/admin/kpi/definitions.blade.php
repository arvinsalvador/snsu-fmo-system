<x-app-layout><x-slot name="header"><h1 class="text-2xl font-semibold">KPI Definitions</h1></x-slot><div class="overflow-hidden rounded-lg border bg-white"><table class="min-w-full divide-y text-sm"><thead><tr><th class="p-3 text-left">KPI</th><th>Category</th><th>Metric</th><th>Direction</th></tr></thead><tbody class="divide-y">@foreach($definitions as $d)<tr><td class="p-3 font-semibold">{{ $d->name }}</td><td>{{ str($d->category)->replace('_',' ')->title() }}</td><td>{{ $d->metric_key }}</td><td>{{ str($d->direction)->replace('_',' ')->title() }}</td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $definitions->links() }}</div></x-app-layout>
