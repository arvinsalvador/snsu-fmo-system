<x-app-layout><x-slot name="header"><h1 class="text-2xl font-semibold">KPI Corrective Actions</h1></x-slot><div class="grid gap-4">@forelse($actions as $a)<section class="rounded-lg border bg-white p-5"><div class="flex justify-between"><strong>{{ $a->title }}</strong><span>{{ str($a->effective_status)->replace('_',' ')->title() }}</span></div><p class="mt-2 text-sm text-gray-500">{{ $a->target->definition->name }} · {{ $a->assignee?->name??'Unassigned' }} · Due {{ $a->due_date?->toDateString()??'not set' }}</p></section>@empty<p class="text-gray-500">No corrective actions.</p>@endforelse</div><div class="mt-4">{{ $actions->links() }}</div></x-app-layout>
