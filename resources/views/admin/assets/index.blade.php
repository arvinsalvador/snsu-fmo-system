<x-app-layout>
    <x-slot name="header"><div class="flex w-full flex-wrap items-start justify-between gap-3"><div><p class="text-sm font-medium text-emerald-700">Asset inventory</p><h1 class="text-2xl font-semibold text-gray-900">Assets</h1></div><div class="flex gap-2"><a href="{{ route('admin.assets.export', request()->query()) }}" class="rounded-md border border-gray-300 px-4 py-2 text-sm font-semibold text-gray-700">Export CSV</a>@can('create', App\Models\Asset::class)<a href="{{ route('admin.assets.create') }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">New asset</a>@endcan</div></div></x-slot>
    <div class="py-6"><div class="mx-auto max-w-7xl space-y-4 px-4 sm:px-6 lg:px-8">
        <form method="GET" class="grid gap-3 rounded-lg border border-gray-200 bg-white p-4 shadow-sm md:grid-cols-[1fr_auto]"><input name="search" value="{{ $filters['search'] ?? '' }}" placeholder="Search asset tag, name, location, brand, model, or serial" class="rounded-md border-gray-300 shadow-sm"><button class="rounded-md bg-gray-900 px-4 py-2 text-sm font-semibold text-white">Filter</button></form>
        <div class="overflow-hidden rounded-md border border-gray-200 bg-white"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left">Asset</th><th class="px-4 py-3 text-left">Category</th><th class="px-4 py-3 text-left">Location</th><th class="px-4 py-3 text-left">Serial</th><th class="px-4 py-3 text-right">Schedules</th><th class="px-4 py-3 text-right">Completions</th><th class="px-4 py-3 text-right">Action</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($assets as $asset)<tr><td class="px-4 py-3"><p class="font-semibold text-gray-900">{{ $asset->asset_tag }}</p><p class="text-gray-500">{{ $asset->name }}</p><p class="text-xs text-gray-400">{{ collect([$asset->brand, $asset->model])->filter()->join(' ') }}</p></td><td class="px-4 py-3 text-gray-600">{{ $asset->category?->name ?? 'Uncategorized' }}</td><td class="px-4 py-3 text-gray-600"><p>{{ $asset->location ?? 'Unassigned' }}</p><p class="text-xs text-gray-400">{{ $asset->building?->name }}{{ $asset->room ? ' / '.$asset->room->room_code : '' }}</p></td><td class="px-4 py-3 text-gray-600">{{ $asset->serial_number ?? '-' }}</td><td class="px-4 py-3 text-right text-gray-700">{{ $asset->maintenance_schedules_count }}</td><td class="px-4 py-3 text-right text-gray-700">{{ $asset->maintenance_records_count }}</td><td class="px-4 py-3 text-right"><div class="flex justify-end gap-3"><a href="{{ route('admin.assets.show', $asset) }}" class="font-medium text-emerald-700">View</a>@can('update', $asset)<a href="{{ route('admin.assets.edit', $asset) }}" class="font-medium text-gray-700">Edit</a>@endcan</div></td></tr>@empty<tr><td colspan="7" class="px-4 py-12 text-center text-gray-500">No assets match the current search.</td></tr>@endforelse</tbody></table></div>{{ $assets->links() }}
    </div></div>
</x-app-layout>
