<x-app-layout>
    <x-slot name="header"><div class="flex w-full items-center justify-between gap-4"><div><p class="text-sm font-medium text-emerald-700">Administration / Assets</p><h1 class="text-2xl font-semibold text-gray-900">Assets</h1></div><a href="{{ route('admin.asset-maintenance.create') }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Record completion</a></div></x-slot>
    <div class="space-y-5">
        <form method="GET" class="grid gap-3 border-b border-gray-200 pb-5 md:grid-cols-[1fr_auto]"><input name="search" value="{{ $filters['search'] ?? '' }}" placeholder="Search asset tag, name, or location" class="rounded-md border-gray-300 text-sm"><button class="rounded-md bg-gray-900 px-4 py-2 text-sm font-semibold text-white">Search</button></form>
        <div class="flex justify-between"><p class="text-sm text-gray-500">{{ $assets->total() }} assets</p><a href="{{ route('admin.asset-maintenance.index') }}" class="text-sm font-semibold text-gray-700">View maintenance history</a></div>
        <div class="overflow-hidden rounded-md border border-gray-200 bg-white"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left">Asset</th><th class="px-4 py-3 text-left">Location</th><th class="px-4 py-3 text-right">Schedules</th><th class="px-4 py-3 text-right">Completions</th><th class="px-4 py-3 text-right">Action</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($assets as $asset)<tr><td class="px-4 py-3"><p class="font-semibold text-gray-900">{{ $asset->asset_tag }}</p><p class="text-gray-500">{{ $asset->name }}</p></td><td class="px-4 py-3 text-gray-600">{{ $asset->location ?? 'Unassigned' }}</td><td class="px-4 py-3 text-right text-gray-700">{{ $asset->maintenance_schedules_count }}</td><td class="px-4 py-3 text-right text-gray-700">{{ $asset->maintenance_records_count }}</td><td class="px-4 py-3 text-right"><a href="{{ route('admin.assets.show', $asset) }}" class="font-medium text-emerald-700">View</a></td></tr>@empty<tr><td colspan="5" class="px-4 py-12 text-center text-gray-500">No assets match the current search.</td></tr>@endforelse</tbody></table></div>{{ $assets->links() }}
    </div>
</x-app-layout>
