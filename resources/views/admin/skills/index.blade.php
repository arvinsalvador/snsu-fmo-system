<x-app-layout>
    <x-slot name="header"><div class="flex w-full items-center justify-between gap-4"><div><p class="text-sm font-medium text-emerald-700">Administration / Skills</p><h1 class="text-2xl font-semibold text-gray-900">Skills catalog</h1></div><a href="{{ route('admin.skills.create') }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Add skill</a></div></x-slot>
    <div class="space-y-5">@if(session('success'))<div class="rounded-md border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif @include('admin.partials.tabs')
        <form method="GET" class="grid gap-3 border-b border-gray-200 pb-5 sm:grid-cols-[1fr_10rem_11rem_9rem_auto]"><input name="search" value="{{ $filters['search'] ?? '' }}" placeholder="Search skills" aria-label="Search skills" class="rounded-md border-gray-300 text-sm"><select name="status" aria-label="Status filter" class="rounded-md border-gray-300 text-sm"><option value="">All statuses</option><option value="active" @selected(($filters['status'] ?? '')==='active')>Active</option><option value="inactive" @selected(($filters['status'] ?? '')==='inactive')>Inactive</option></select><select name="sort" aria-label="Sort skills" class="rounded-md border-gray-300 text-sm"><option value="name">Name</option><option value="created_at" @selected(($filters['sort'] ?? '')==='created_at')>Created date</option><option value="is_active" @selected(($filters['sort'] ?? '')==='is_active')>Status</option></select><select name="per_page" aria-label="Rows per page" class="rounded-md border-gray-300 text-sm">@foreach([10,15,25,50] as $number)<option value="{{ $number }}" @selected((int)($filters['per_page'] ?? 15)===$number)>{{ $number }} rows</option>@endforeach</select><button class="rounded-md bg-gray-900 px-4 py-2 text-sm font-semibold text-white">Apply</button></form>
        <div class="flex justify-between"><p class="text-sm text-gray-500">{{ $skills->total() }} skills</p><a href="{{ route('admin.skills.export', request()->query()) }}" class="text-sm font-semibold text-gray-700">Export CSV</a></div>
        <div class="overflow-hidden rounded-md border border-gray-200 bg-white"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left">Skill</th><th class="px-4 py-3 text-left">Staff</th><th class="px-4 py-3 text-left">Status</th><th class="px-4 py-3 text-right">Actions</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($skills as $skill)<tr><td class="px-4 py-3"><p class="font-semibold">{{ $skill->name }}</p><p class="max-w-xl truncate text-gray-500">{{ $skill->description ?: 'No description' }}</p></td><td class="px-4 py-3 text-gray-600">{{ $skill->staff_profiles_count }}</td><td class="px-4 py-3"><span class="rounded-full px-2 py-1 text-xs font-semibold {{ $skill->is_active?'bg-emerald-50 text-emerald-700':'bg-gray-100 text-gray-600' }}">{{ $skill->is_active?'Active':'Inactive' }}</span></td><td class="px-4 py-3 text-right"><a href="{{ route('admin.skills.show', $skill) }}" class="font-medium text-gray-600">View</a><a href="{{ route('admin.skills.edit', $skill) }}" class="ml-3 font-medium text-emerald-700">Edit</a></td></tr>@empty<tr><td colspan="4" class="px-4 py-12 text-center text-gray-500">No skills match the current filters.</td></tr>@endforelse</tbody></table></div></div>{{ $skills->links() }}
    </div>
</x-app-layout>
