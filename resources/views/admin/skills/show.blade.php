<x-app-layout>
    <x-slot name="header"><div class="flex w-full items-center justify-between gap-4"><div><p class="text-sm font-medium text-emerald-700">Administration / Skills</p><h1 class="text-2xl font-semibold text-gray-900">{{ $skill->name }}</h1></div><a href="{{ route('admin.skills.edit', $skill) }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Edit skill</a></div></x-slot>
    <div class="mx-auto max-w-4xl space-y-5">@if(session('success'))<div class="rounded-md border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif @include('admin.partials.tabs')
        <div class="rounded-md border border-gray-200 bg-white"><dl class="grid sm:grid-cols-2"><div class="border-b border-gray-100 px-5 py-4"><dt class="text-xs font-semibold uppercase text-gray-500">Status</dt><dd class="mt-1 text-sm font-medium">{{ $skill->is_active ? 'Active' : 'Inactive' }}</dd></div><div class="border-b border-gray-100 px-5 py-4"><dt class="text-xs font-semibold uppercase text-gray-500">Assigned staff</dt><dd class="mt-1 text-sm font-medium">{{ $skill->staff_profiles_count }}</dd></div><div class="border-b border-gray-100 px-5 py-4 sm:col-span-2"><dt class="text-xs font-semibold uppercase text-gray-500">Description</dt><dd class="mt-1 text-sm text-gray-700">{{ $skill->description ?: 'No description provided.' }}</dd></div></dl>
            <div class="px-5 py-4"><h2 class="text-sm font-semibold text-gray-900">Staff with this skill</h2><div class="mt-3 flex flex-wrap gap-2">@forelse($skill->staffProfiles as $profile)<a href="{{ route('admin.staff.show', $profile) }}" class="rounded-full bg-gray-100 px-3 py-1 text-sm text-gray-700">{{ $profile->user?->name }}</a>@empty<p class="text-sm text-gray-500">No staff profiles currently use this skill.</p>@endforelse</div></div>
        </div>
    </div>
</x-app-layout>
