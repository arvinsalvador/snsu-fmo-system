<x-app-layout>
    <x-slot name="header"><div class="flex w-full items-center justify-between gap-4"><div><p class="text-sm font-medium text-emerald-700">Administration / Inventory</p><h1 class="text-2xl font-semibold text-gray-900">Consumable inventory</h1></div>@can('create', App\Models\InventoryItem::class)<a href="{{ route('admin.inventory.create') }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Add item</a>@endcan</div></x-slot>
    <div class="space-y-5">@if(session('success'))<div class="rounded-md border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
        <form method="GET" class="grid gap-3 border-b border-gray-200 pb-5 lg:grid-cols-[1fr_13rem_10rem_10rem_11rem_9rem_auto]"><input name="search" value="{{ $filters['search'] ?? '' }}" placeholder="Search items" class="rounded-md border-gray-300 text-sm"><select name="category_id" class="rounded-md border-gray-300 text-sm"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected((string)($filters['category_id'] ?? '') === (string)$category->id)>{{ $category->name }}</option>@endforeach</select><select name="status" class="rounded-md border-gray-300 text-sm"><option value="">All statuses</option><option value="active" @selected(($filters['status'] ?? '')==='active')>Active</option><option value="inactive" @selected(($filters['status'] ?? '')==='inactive')>Inactive</option></select><select name="stock" class="rounded-md border-gray-300 text-sm"><option value="">All stock</option><option value="low" @selected(($filters['stock'] ?? '')==='low')>Low stock</option></select><select name="sort" class="rounded-md border-gray-300 text-sm"><option value="name">Name</option><option value="item_code" @selected(($filters['sort'] ?? '')==='item_code')>Code</option><option value="current_stock" @selected(($filters['sort'] ?? '')==='current_stock')>Current stock</option><option value="minimum_stock" @selected(($filters['sort'] ?? '')==='minimum_stock')>Minimum stock</option><option value="created_at" @selected(($filters['sort'] ?? '')==='created_at')>Created date</option></select><select name="per_page" class="rounded-md border-gray-300 text-sm">@foreach([10,15,25,50] as $number)<option value="{{ $number }}" @selected((int)($filters['per_page'] ?? 15)===$number)>{{ $number }} rows</option>@endforeach</select><button class="rounded-md bg-gray-900 px-4 py-2 text-sm font-semibold text-white">Apply</button></form>
        <div class="flex justify-between"><p class="text-sm text-gray-500">{{ $items->total() }} items</p>@can('export', App\Models\InventoryItem::class)<a href="{{ route('admin.inventory.export', request()->query()) }}" class="text-sm font-semibold text-gray-700">Export CSV</a>@endcan</div>
        <div class="overflow-hidden rounded-md border border-gray-200 bg-white"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left">Item</th><th class="px-4 py-3 text-left">Category</th><th class="px-4 py-3 text-right">Stock</th><th class="px-4 py-3 text-left">Status</th><th class="px-4 py-3 text-right">Actions</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($items as $item)<tr><td class="px-4 py-3"><p class="font-semibold text-gray-900">{{ $item->name }}</p><p class="text-gray-500">{{ $item->item_code }}{{ $item->brand ? ' / '.$item->brand : '' }}</p></td><td class="px-4 py-3 text-gray-600">{{ $item->category?->name }}</td><td class="px-4 py-3 text-right"><p class="font-semibold {{ $item->is_low_stock ? 'text-red-700' : 'text-gray-900' }}">{{ number_format((float)$item->current_stock, 2) }} {{ $item->unit }}</p><p class="text-xs text-gray-500">Min {{ number_format((float)$item->minimum_stock, 2) }}</p></td><td class="px-4 py-3"><span class="rounded-full px-2 py-1 text-xs font-semibold {{ $item->status === 'active' ? 'bg-emerald-50 text-emerald-700' : 'bg-gray-100 text-gray-600' }}">{{ ucfirst($item->status) }}</span>@if($item->is_low_stock)<span class="ml-2 rounded-full bg-red-50 px-2 py-1 text-xs font-semibold text-red-700">Low</span>@endif</td><td class="px-4 py-3 text-right"><a href="{{ route('admin.inventory.show', $item) }}" class="font-medium text-gray-600">View</a>@can('update', $item)<a href="{{ route('admin.inventory.edit', $item) }}" class="ml-3 font-medium text-emerald-700">Edit</a>@endcan</td></tr>@empty<tr><td colspan="5" class="px-4 py-12 text-center text-gray-500">No inventory items match the current filters.</td></tr>@endforelse</tbody></table></div></div>{{ $items->links() }}
    </div>
</x-app-layout>