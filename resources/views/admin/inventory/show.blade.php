<x-app-layout>
    <x-slot name="header"><div class="flex w-full items-center justify-between gap-4"><div><p class="text-sm font-medium text-emerald-700">Administration / Inventory</p><h1 class="text-2xl font-semibold text-gray-900">{{ $item->name }}</h1></div>@can('update', $item)<a href="{{ route('admin.inventory.edit', $item) }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Edit item</a>@endcan</div></x-slot>
    <div class="space-y-5">@if(session('success'))<div class="rounded-md border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
        <div class="grid gap-5 lg:grid-cols-[1fr_24rem]"><div class="rounded-md border border-gray-200 bg-white"><dl class="grid sm:grid-cols-2"><div class="border-b border-gray-100 px-5 py-4"><dt class="text-xs font-semibold uppercase text-gray-500">Code</dt><dd class="mt-1 text-sm font-medium">{{ $item->item_code }}</dd></div><div class="border-b border-gray-100 px-5 py-4"><dt class="text-xs font-semibold uppercase text-gray-500">Category</dt><dd class="mt-1 text-sm font-medium">{{ $item->category?->name }}</dd></div><div class="border-b border-gray-100 px-5 py-4"><dt class="text-xs font-semibold uppercase text-gray-500">Current stock</dt><dd class="mt-1 text-sm font-semibold {{ $item->is_low_stock ? 'text-red-700' : 'text-gray-900' }}">{{ number_format((float)$item->current_stock, 2) }} {{ $item->unit }}</dd></div><div class="border-b border-gray-100 px-5 py-4"><dt class="text-xs font-semibold uppercase text-gray-500">Minimum stock</dt><dd class="mt-1 text-sm font-medium">{{ number_format((float)$item->minimum_stock, 2) }}</dd></div><div class="border-b border-gray-100 px-5 py-4 sm:col-span-2"><dt class="text-xs font-semibold uppercase text-gray-500">Remarks</dt><dd class="mt-1 text-sm text-gray-700">{{ $item->remarks ?: 'No remarks provided.' }}</dd></div></dl></div>
        @can('adjust', $item)<div class="space-y-5"><form method="POST" action="{{ route('admin.inventory.stock-in', $item) }}" class="space-y-4 rounded-md border border-gray-200 bg-white p-5">@csrf<h2 class="text-sm font-semibold text-gray-900">Stock in</h2><input name="quantity" type="number" step="0.01" min="0.01" class="w-full rounded-md border-gray-300 text-sm" placeholder="Quantity"><textarea name="remarks" rows="3" class="w-full rounded-md border-gray-300 text-sm" placeholder="Remarks"></textarea><button class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Receive stock</button></form><form method="POST" action="{{ route('admin.inventory.adjust', $item) }}" class="space-y-4 rounded-md border border-gray-200 bg-white p-5">@csrf<h2 class="text-sm font-semibold text-gray-900">Stock adjustment</h2><div class="grid gap-3 sm:grid-cols-2"><select name="direction" class="rounded-md border-gray-300 text-sm"><option value="increase">Increase</option><option value="decrease">Decrease</option></select><input name="quantity" type="number" step="0.01" min="0.01" class="rounded-md border-gray-300 text-sm" placeholder="Quantity"></div><textarea name="remarks" rows="3" class="w-full rounded-md border-gray-300 text-sm" placeholder="Reason"></textarea><button class="rounded-md bg-gray-900 px-4 py-2 text-sm font-semibold text-white">Apply adjustment</button></form></div>@endcan</div>
        <div class="rounded-md border border-gray-200 bg-white"><div class="border-b border-gray-200 px-5 py-4"><h2 class="text-sm font-semibold text-gray-900">Stock movement history</h2></div><div class="overflow-x-auto"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left">Date</th><th class="px-4 py-3 text-left">Type</th><th class="px-4 py-3 text-right">Quantity</th><th class="px-4 py-3 text-right">Before</th><th class="px-4 py-3 text-right">After</th><th class="px-4 py-3 text-left">Remarks</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($movements as $movement)<tr><td class="px-4 py-3 text-gray-600">{{ $movement->occurred_at?->format('Y-m-d H:i') }}</td><td class="px-4 py-3">{{ str_replace('_', ' ', ucfirst($movement->movement_type)) }}</td><td class="px-4 py-3 text-right font-medium">{{ number_format((float)$movement->quantity, 2) }}</td><td class="px-4 py-3 text-right">{{ number_format((float)$movement->stock_before, 2) }}</td><td class="px-4 py-3 text-right">{{ number_format((float)$movement->stock_after, 2) }}</td><td class="px-4 py-3 text-gray-600">{{ $movement->remarks ?: 'No remarks' }}</td></tr>@empty<tr><td colspan="6" class="px-4 py-10 text-center text-gray-500">No stock movements recorded yet.</td></tr>@endforelse</tbody></table></div><div class="px-5 py-4">{{ $movements->links() }}</div></div>
    </div>
</x-app-layout>