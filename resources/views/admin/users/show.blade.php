<x-app-layout>
    <x-slot name="header"><div class="flex w-full items-center justify-between"><div><p class="text-sm font-medium text-emerald-700">Administration / Users</p><h1 class="text-2xl font-semibold text-gray-900">{{ $user->name }}</h1></div><a href="{{ route('admin.users.edit',$user) }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Edit user</a></div></x-slot>
    <div class="mx-auto max-w-4xl space-y-5">@if(session('success'))<div class="rounded-md border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif @include('admin.partials.tabs')
        <div class="rounded-md border border-gray-200 bg-white"><dl class="grid sm:grid-cols-2">@foreach(['Email'=>$user->email,'Username'=>$user->username,'Employee number'=>$user->employee_no,'Student number'=>$user->student_no,'Mobile number'=>$user->mobile_number,'Roles'=>$user->roles->pluck('name')->implode(', '),'Status'=>$user->is_active?'Active':'Inactive','Last login'=>$user->last_login_at?->format('M j, Y g:i A')] as $label=>$value)<div class="border-b border-gray-100 px-5 py-4"><dt class="text-xs font-semibold uppercase text-gray-500">{{ $label }}</dt><dd class="mt-1 text-sm font-medium text-gray-900">{{ $value?:'—' }}</dd></div>@endforeach</dl>
            <div class="flex items-center justify-between px-5 py-4"><a href="{{ route('admin.users.index') }}" class="text-sm font-semibold text-gray-600">Back to users</a>@if($user->is_active)<form method="POST" action="{{ route('admin.users.deactivate',$user) }}">@csrf @method('PATCH')<button class="text-sm font-semibold text-red-600" @disabled(auth()->user()->is($user))>Deactivate account</button></form>@else<form method="POST" action="{{ route('admin.users.activate',$user) }}">@csrf @method('PATCH')<button class="text-sm font-semibold text-emerald-700">Activate account</button></form>@endif</div>
        </div>
    </div>
</x-app-layout>
