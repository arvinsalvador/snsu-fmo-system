<x-app-layout>
    <x-slot name="header"><div class="flex w-full items-center justify-between gap-4"><div><p class="text-sm font-medium text-emerald-700">Administration / Assets</p><h1 class="text-2xl font-semibold text-gray-900">Maintenance completion history</h1></div>@can('create', App\Models\AssetMaintenanceRecord::class)<a href="{{ route('admin.asset-maintenance.create') }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Record completion</a>@endcan</div></x-slot>
    <div class="space-y-5">@if(session('success'))<div class="rounded-md border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
        <form method="GET" class="grid gap-3 border-b border-gray-200 pb-5 lg:grid-cols-[1fr_12rem_12rem_10rem_10rem_9rem_auto]"><input name="search" value="{{ $filters['search'] ?? '' }}" placeholder="Search assets, work orders, findings" class="rounded-md border-gray-300 text-sm"><select name="asset_id" class="rounded-md border-gray-300 text-sm"><option value="">All assets</option>@foreach($assets as $asset)<option value="{{ $asset->id }}" @selected((string)($filters['asset_id'] ?? '') === (string)$asset->id)>{{ $asset->asset_tag }}</option>@endforeach</select><select name="staff_profile_id" class="rounded-md border-gray-300 text-sm"><option value="">All staff</option>@foreach($staff as $person)<option value="{{ $person->id }}" @selected((string)($filters['staff_profile_id'] ?? '') === (string)$person->id)>{{ $person->user?->name ?? $person->employee_code }}</option>@endforeach</select><input type="date" name="completed_from" value="{{ $filters['completed_from'] ?? '' }}" class="rounded-md border-gray-300 text-sm"><input type="date" name="completed_to" value="{{ $filters['completed_to'] ?? '' }}" class="rounded-md border-gray-300 text-sm"><select name="per_page" class="rounded-md border-gray-300 text-sm">@foreach([15,25,50,100] as $number)<option value="{{ $number }}" @selected((int)($filters['per_page'] ?? 15) === $number)>{{ $number }} rows</option>@endforeach</select><button class="rounded-md bg-gray-900 px-4 py-2 text-sm font-semibold text-white">Apply</button></form>
        <div class="flex justify-between"><p class="text-sm text-gray-500">{{ $records->total() }} records</p>@can('export', App\Models\AssetMaintenanceRecord::class)<a href="{{ route('admin.asset-maintenance.export', request()->query()) }}" class="text-sm font-semibold text-gray-700">Export CSV</a>@endcan</div>
        <div class="overflow-hidden rounded-md border border-gray-200 bg-white"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left">Date</th><th class="px-4 py-3 text-left">Asset</th><th class="px-4 py-3 text-left">Technician</th><th class="px-4 py-3 text-left">Linked work</th><th class="px-4 py-3 text-left">Actions</th><th class="px-4 py-3 text-right">Labor</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($records as $record)<tr><td class="px-4 py-3"><a href="{{ route('admin.asset-maintenance.show', $record) }}" class="font-semibold text-gray-900">{{ $record->completion_date->format('M j, Y') }}</a></td><td class="px-4 py-3"><p class="font-medium text-gray-900">{{ $record->asset?->asset_tag }}</p><p class="text-gray-500">{{ $record->asset?->name }}</p></td><td class="px-4 py-3 text-gray-600">{{ $record->staffProfile?->user?->name ?? 'Not assigned' }}</td><td class="px-4 py-3 text-gray-600">{{ $record->maintenanceSchedule?->title ?? $record->workOrder?->work_order_number ?? 'Manual' }}</td><td class="px-4 py-3 text-gray-700">{{ str($record->actions_taken)->limit(80) }}</td><td class="px-4 py-3 text-right text-gray-700">{{ $record->labor_cost ? number_format((float) $record->labor_cost, 2) : '-' }}</td></tr>@empty<tr><td colspan="6" class="px-4 py-12 text-center text-gray-500">No maintenance records match the current filters.</td></tr>@endforelse</tbody></table></div></div>{{ $records->links() }}
    </div>
</x-app-layout>
