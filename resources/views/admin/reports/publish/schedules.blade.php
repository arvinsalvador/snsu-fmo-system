<x-app-layout><x-slot name="header"><div class="flex justify-between"><h1 class="text-2xl font-semibold">Scheduled reports</h1><a href="{{ route('admin.reports.schedules.create') }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">New schedule</a></div></x-slot><div class="overflow-hidden rounded-lg border bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left">Schedule</th><th>Frequency</th><th>Next run</th><th>Recipients</th><th>Status</th><th></th></tr></thead><tbody class="divide-y">@forelse($schedules as $schedule)<tr><td class="px-4 py-3"><p class="font-semibold">{{ $schedule->name }}</p><p class="text-xs text-gray-500">{{ $schedule->template->name }}</p></td><td>{{ str($schedule->frequency)->title() }}</td><td>{{ $schedule->next_run_at?->timezone($schedule->timezone)->format('M j, Y g:i A')??'Disabled' }}</td><td>{{ $schedule->recipients->count() }}</td><td>{{ $schedule->is_active?'Active':'Inactive' }}</td><td><a class="font-semibold text-emerald-700" href="{{ route('admin.reports.schedules.edit',$schedule) }}">Manage</a></td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-gray-500">No report schedules configured.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $schedules->links() }}</div></x-app-layout>
