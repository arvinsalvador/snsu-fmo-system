<x-app-layout><x-slot name="header"><div class="flex justify-between"><h1 class="text-2xl font-semibold">Report templates</h1>@can('create',App\Models\ReportTemplate::class)<a href="{{ route('admin.reports.templates.create') }}" class="rounded-md bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">New template</a>@endcan</div></x-slot><div class="grid gap-4 md:grid-cols-2">@foreach($templates as $template)<a href="{{ route('admin.reports.templates.edit',$template) }}" class="rounded-lg border bg-white p-5 shadow-sm"><div class="flex justify-between"><h2 class="font-semibold">{{ $template->name }}</h2><span class="text-xs text-gray-500">{{ $template->is_system?'System':'Custom' }}</span></div><p class="mt-2 text-sm text-gray-500">{{ str($template->report_type)->replace('_',' ')->title() }} · {{ strtoupper($template->output_format) }}</p></a>@endforeach</div><div class="mt-5">{{ $templates->links() }}</div></x-app-layout>
